const express = require("express");
const router = express.Router();

// Archivo intencionalmente vacío respecto a relaciones usuarios/documentos.
// Mantengo un router mínimo para no romper importaciones desde app.js.

module.exports = router;
